{
  "name": "batch-bump",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "description": "Bulk operations across multiple repos: install/remove npm packages or execute any shell command",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^14.0.1",
    "p-limit": "^7.1.1",
    "simple-git": "^3.28.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/*.spec.js"
    ]
  }
}
